<%= form_with(model: band_name, local: true) do |form| %>
  <% if band_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(band_name.errors.count, "error") %> prohibited this band_name from being saved:</h2>

      <ul>
        <% band_name.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
